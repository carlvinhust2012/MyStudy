jdbc操作数据库过于繁琐，所以有了hibernate，hibernate过于笨重，所以有了轻量级的mybatis，mybatis手写SQL比较麻烦，所以有了mapper、maybatis plus。
structs2浪费资源，所以有了单例的springMVC

软件架构--软件架构师在软件内部，经过综合各种因素，选择特定技术，将系统划分为不同部分，并使这些部分互相分工，彼此协作，为用户提供价值。
考虑的因素：业务需求、技术栈、成本、组织结构、可扩展性、可维护性

一层机构-->MVC三层架构-->dubbo(不是微服务)
